# ───── Local secrets / env (never commit) ──────────────────────────────────────
.secrets
.env
.env.*
*.secret
*.secrets

# ───── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
ENV/
env/
.python-version
.ipynb_checkpoints/

# Packaging/build
build/
dist/
*.egg-info/
.eggs/
wheels/
pip-wheel-metadata/

# Testing & coverage
.coverage
.coverage.*
.pytest_cache/
.tox/
.nox/
coverage.xml
htmlcov/

# Linters / type checkers
.mypy_cache/
.pytype/
.ruff_cache/
.pyre/

# Logs
*.log
logs/

# ───── Editors / IDEs (remove .vscode/ if you want to keep workspace settings) ─
.vscode/
.idea/
*.code-workspace

# OS files
.DS_Store
Thumbs.db

# ───── Docker / Compose ────────────────────────────────────────────────────────
docker-compose.override.yml
*.local.yml
*.override.yml
.env.docker
.env.local

# ───── Ansible ─────────────────────────────────────────────────────────────────
*.retry

# ───── Local data/artifacts (created during experiments) ───────────────────────
signoz-data/
mysql-data/
tmp/
tmp/**

# ───── nektos/act local cache (if any) ─────────────────────────────────────────
.act/

# NOTE: Flyway migrations are KEPT (do not ignore them)
# (We're not ignoring *.sql, but keep these lines in case you add broader rules later)
!migrations/
!migrations/**/*.sql
